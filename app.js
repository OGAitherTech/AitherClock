const $=id=>document.getElementById(id);const pad=n=>String(n).padStart(2,'0');let settings=JSON.parse(localStorage.getItem('aither-settings')||'{}');let format=settings.format||'12',showSeconds=settings.showSeconds!==false;function save(){localStorage.setItem('aither-settings',JSON.stringify({format,showSeconds,clockStyle:$('clockStyle').value}))}function tick(){const d=new Date(),h=d.getHours(),m=d.getMinutes(),s=d.getSeconds(),ms=d.getMilliseconds();let dh=format==='12'?((h+11)%12)+1:h;$('digital').textContent=`${pad(dh)}:${pad(m)}${showSeconds?':'+pad(s):''}`;$('period').textContent=format==='12'?(h>=12?'PM':'AM'):'';$('date').textContent=d.toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric',year:'numeric'});$('hourHand').style.transform=`translateX(-50%) rotate(${(h%12)*30+m*.5}deg)`;$('minuteHand').style.transform=`translateX(-50%) rotate(${m*6+s*.1}deg)`;$('secondHand').style.transform=`translateX(-50%) rotate(${s*6+ms*.006}deg)`;renderWorld(d);checkAlarms(d);setTimeout(tick,250)}tick();$('format').value=format;$('seconds').value=showSeconds?'show':'hide';$('clockStyle').value=settings.clockStyle||'analog';document.body.classList.toggle('digital-only',$('clockStyle').value==='digital');$('format').onchange=e=>{format=e.target.value;save()};$('seconds').onchange=e=>{showSeconds=e.target.value==='show';save()};$('clockStyle').onchange=e=>{document.body.classList.toggle('digital-only',e.target.value==='digital');save()};$('themeBtn').onclick=()=>{document.body.classList.toggle('light');localStorage.setItem('aither-theme',document.body.classList.contains('light')?'light':'dark');$('themeBtn').textContent=document.body.classList.contains('light')?'Light':'Dark'};if(localStorage.getItem('aither-theme')==='light'){document.body.classList.add('light');$('themeBtn').textContent='Light'}$('fullscreenBtn').onclick=()=>{if(!document.fullscreenElement)document.documentElement.requestFullscreen?.();else document.exitFullscreen?.()};document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.panel').forEach(x=>x.classList.remove('active-panel'));b.classList.add('active');$(b.dataset.panel).classList.add('active-panel')});
const zones=[['New York','America/New_York'],['London','Europe/London'],['Tokyo','Asia/Tokyo'],['Sydney','Australia/Sydney'],['Los Angeles','America/Los_Angeles'],['Paris','Europe/Paris']];function renderWorld(d){$('worldClocks').innerHTML=zones.map(([name,tz])=>{const time=d.toLocaleTimeString(undefined,{timeZone:tz,hour:'2-digit',minute:'2-digit',second:'2-digit'});const date=d.toLocaleDateString(undefined,{timeZone:tz,weekday:'short',month:'short',day:'numeric'});return `<div class="world-card"><strong>${name}</strong><small>${date}</small><div class="world-time">${time}</div></div>`}).join('')}
let swStartTime=0,swElapsed=0,swTimer=null;function swRender(){const t=swElapsed+(swTimer?Date.now()-swStartTime:0),cs=Math.floor(t/10)%100,sec=Math.floor(t/1000)%60,min=Math.floor(t/60000);$('stopwatch').textContent=`${pad(min)}:${pad(sec)}.${pad(cs)}`}$('swStart').onclick=()=>{if(swTimer){swElapsed+=Date.now()-swStartTime;clearInterval(swTimer);swTimer=null;$('swStart').textContent='Start'}else{swStartTime=Date.now();swTimer=setInterval(swRender,20);$('swStart').textContent='Pause'}};$('swReset').onclick=()=>{clearInterval(swTimer);swTimer=null;swElapsed=0;$('swStart').textContent='Start';$('laps').innerHTML='';swRender()};$('swLap').onclick=()=>{if(swTimer){const li=document.createElement('li');li.textContent=$('stopwatch').textContent;$('laps').prepend(li)}};swRender();
let timerTotal=300,timerLeft=300,timerInt=null;function readTimer(){const m=Math.max(0,parseInt($('timerMin').value)||0),s=Math.min(59,Math.max(0,parseInt($('timerSec').value)||0));timerTotal=m*60+s;timerLeft=timerTotal;timerRender()}function timerRender(){$('timerDisplay').textContent=`${pad(Math.floor(timerLeft/60))}:${pad(timerLeft%60)}`;$('timerProgress').style.width=timerTotal?`${Math.max(0,timerLeft/timerTotal*100)}%`:'0%'}$('timerMin').oninput=readTimer;$('timerSec').oninput=readTimer;$('timerStart').onclick=()=>{if(timerInt){clearInterval(timerInt);timerInt=null;$('timerStart').textContent='Start'}else{if(timerLeft<=0)readTimer();$('timerStart').textContent='Pause';timerInt=setInterval(()=>{timerLeft--;timerRender();if(timerLeft<=0){clearInterval(timerInt);timerInt=null;$('timerStart').textContent='Start';try{navigator.vibrate?.([180,80,180,80,300])}catch{}alert('Aither Clock: Timer finished.')}},1000)}};$('timerReset').onclick=()=>{clearInterval(timerInt);timerInt=null;$('timerStart').textContent='Start';readTimer()};timerRender();
let alarms=JSON.parse(localStorage.getItem('aither-alarms')||'[]');function saveAlarms(){localStorage.setItem('aither-alarms',JSON.stringify(alarms));renderAlarms()}function renderAlarms(){if(!alarms.length){$('alarms').innerHTML='<div class="empty">No alarms yet. Add one for a reminder.</div>';return}$('alarms').innerHTML=alarms.map(a=>`<div class="alarm-card"><div class="alarm-info"><strong>${a.time}</strong><small>${a.label||'Alarm'}${a.enabled?'':' • Off'}</small></div><div class="alarm-actions"><button onclick="toggleAlarm(${a.id})">${a.enabled?'On':'Off'}</button><button onclick="deleteAlarm(${a.id})">Delete</button></div></div>`).join('')}function addAlarm(){let time=prompt('Alarm time (for example 7:30 AM or 19:30):');if(!time)return;let label=prompt('Label (optional):')||'Alarm';alarms.push({id:Date.now(),time,label,enabled:true,last:''});saveAlarms()}function toggleAlarm(id){const a=alarms.find(x=>x.id===id);if(a){a.enabled=!a.enabled;saveAlarms()}}function deleteAlarm(id){alarms=alarms.filter(x=>x.id!==id);saveAlarms()}function checkAlarms(d){const now=d.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});const now24=pad(d.getHours())+':'+pad(d.getMinutes());alarms.forEach(a=>{if(!a.enabled||a.last===now24)return;let target=a.time.trim().toUpperCase();let match=target===now24||target===now||target===d.toLocaleTimeString([], {hour:'numeric',minute:'2-digit'});if(match){a.last=now24;localStorage.setItem('aither-alarms',JSON.stringify(alarms));try{navigator.vibrate?.([250,100,250])}catch{}alert(`Aither Clock alarm: ${a.label}`)}})}$('addAlarm').onclick=addAlarm;renderAlarms();